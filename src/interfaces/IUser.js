export default class IUser {

    constructor(avatarUrl, name, id, username) {

        this.avatarUrl = avatarUrl;
        this.name = name;
        this.id = id; 
        this.username = username
    };
}